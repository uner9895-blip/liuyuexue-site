# 花笺相册 / 影像小记 资源目录

这是用于存放静态个人网站相册图片的目录。

## 如何使用

您可以将自己拍摄或设计的照片（如生活照、练习插画或自然风景图）放入本目录，并在对应的 `about.html` 文件中的相册卡片（`.gallery-card img`）中修改对应的路径。

### 推荐规格
- 格式：`jpg`, `png`, `webp`
- 比例：`4:3` 或 `16:10`（推荐统一比例以保持中式卡片排版的协调）
- 大小：单张图片建议控制在 `300KB` 以内以保障网页加载速度
