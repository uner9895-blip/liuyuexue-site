# 听雪小筑 / 一曲清音 音乐资源目录

这是用于存放静态网站音乐音频文件的目录。

## 如何使用

1. 将本地音乐文件放入本目录，当前歌单使用本目录中的七个 `.mp3` 文件。
2. 播放路径由 `main.js` 中的 `musicTracks` 配置控制，默认读取当前选中的本地曲目。
3. 本项目已经预留好对应的 `<audio>` 标签与古典 UI 交互，用户点击风铃/玉坠挂件或听雪页播放按钮即可播放。

### 推荐规格
- 格式：`mp3`, `m4a`, `ogg`
- 建议：建议选择循环播放性好、意境安宁克制的纯音乐。
